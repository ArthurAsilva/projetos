import React from "react";

function Footer() {
  return (
    <footer
      style={{
        background: "#000",
        padding: "20px 10px",
        color: "#f5f5f5",
        textAlign: "center",
        fontFamily: "Arial, sans-serif",
        fontSize: "14px",
        borderTop: "2px solid #1e90ff",
        boxShadow: "0 -2px 8px rgba(0, 0, 0, 0.7)",
      }}
    >
      <p style={{ margin: 0, color: "#1e90ff", fontWeight: "bold" }}>
        ATV - ARTHUR ANDRE
      </p>
    </footer>
  );
}

export default Footer;
